function solve(data) {

    let firstElement = parseInt(data[0]);

    let secondElement = parseInt(data[data.length - 1]);

    console.log(`${firstElement + secondElement}`);

}

solve(['20', '30', '40']);